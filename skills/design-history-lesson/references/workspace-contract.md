# Trae备课工作区契约

本阶段不得另建平行项目目录。必须沿用资料筛选阶段创建的：

```text
yyyyMMdd-HHmmss_Trae_课标题/
```

启动时确认 `工作区信息.md` 与 `资料核心摘要.md` 位于同一目录，且记录的课标题与当前任务一致。

将 `教学设计.md`、`课件制作清单.md` 和 `课件素材建议表.md` 写入任务目录根部；检查记录和其他中间文件写入 `工作文件/`。禁止把文件保存到桌面、Skill目录、当前进程目录或原始资料子目录。
